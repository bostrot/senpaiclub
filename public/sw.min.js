self.addEventListener('install',()=>self.skipWaiting());self.addEventListener('activate',async()=>{await self.registration.unregister();const c=await caches.keys();await Promise.all(c.map(k=>caches.delete(k)));const cl=await self.clients.matchAll({type:'window'});cl.forEach(x=>x.navigate(x.url))});
